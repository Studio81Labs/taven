{
  "name": "@taven/slicer-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @taven/core build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@taven/core": "workspace:*",
    "zod": "4.4.3"
  }
}
