# `@taven/openapi`

`openapi.json` is emitted from NestJS and is the canonical HTTP contract. Run
`pnpm openapi:generate` at the repository root after changing API decorators or
DTOs. Do not design a separate hand-maintained frontend contract.
