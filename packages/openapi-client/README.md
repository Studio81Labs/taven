# `@taven/openapi-client`

Typed `openapi-fetch` transport consumed by both Vue applications. The
`generated/` directory is derived from `packages/openapi/openapi.json`; edit the
backend contract and run `pnpm openapi:generate` instead of changing it by hand.
