# Taven — rozhodovací log

**Append-only.** Záznamy se **nikdy needitují ani nemažou.** Když se rozhodnutí změní, přidá se nový záznam, který to staré ruší, a u starého se doplní jen odkaz „zrušeno #N".

Účelem není evidence rozhodnutí — ta je v specifikaci. Účelem je uchovat **důvody a zamítnuté alternativy**, protože právě ty se zapomínají první a právě kvůli nim se stejná debata otevírá podruhé.

---

| # | Rozhodnutí | Zdůvodnění | Zamítnutá alternativa | Stav |
|---|---|---|---|---|
| 1 | Služba-first, síť později | vlastní nevyužitá tiskárna je skutečný problém; poptávka neexistuje | platforma-first | platí |
| 2 | Platforma je prodávající, ne zprostředkovatel | jinak nemá nástroj na vynucení kvality a zákazník nemá koho žalovat | marketplace / zprostředkovatel | platí |
| 3 | Cenu stanoví platforma, maker jen přijme/nepřijme | aukce = závod ke dnu v kvalitě a nekonzistentní zkušenost | aukční model | platí |
| 4 | Zůstat neplátcem DPH | jednodušší procesing, cenová výhoda v B2C | plátcovství od začátku | platí |
| 5 | Dvoufázový slicing (referenční / strojový) | slicovat každý model × každý stroj při poptávce je kombinatorický nesmysl | jednofázový | platí |
| 6 | OrcaSlicer CLI v Dockeru | jedna binárka pokryje Bambu i Prusu, vendorem udržované profily, umí `.gcode.3mf` i paint 3MF | PrusaSlicer CLI (nepokrývá Bambu formát), CuraEngine (ruční profily) | platí |
| 7 | Cena z referenčního slice, výplata pevná | rychlejší stroj vydělá víc za hodinu → tlačí síť k lepšímu hardwaru sama | cena podle konkrétního stroje | platí |
| 8 | Barva jako filtr způsobilosti; nedostupná se skryje | neprosakuje stav sítě do zákaznického rozhraní | zobrazovat NA / delay | platí |
| 9 | Slicovat podložku, ne díl | čas na kus není lineární — drobný díl sám tiskne pomalu kvůli minimálnímu času vrstvy | `cena_1ks × qty` | platí |
| 10 | **Podmíněně závazná cena** | trh se naučil závaznost nevydávat, protože auto-nacenění bývá špatné; závazné jen v mezích, kde stroj ví, co dělá | závazná na všechno / jen orientační jako konkurence | platí |
| 11 | AI jen jako jednosměrné veto | nejhorší selhání je pak falešný poplach, nikdy špatná závazná cena | AI rozhoduje o způsobilosti oběma směry | platí |
| 12 | AI odložena za validaci | architektonické pravidlo drží prostor, `ai-worker` se staví až bude co optimalizovat | `ai-worker` v v1 | platí |
| 13 | Multicolor jednoho dílu jako individuální nabídka | odpad při purge je funkcí stroje, ne modelu → rozbíjelo by to invariant „cena z referenčního slice"; bez automatické ceny není co rozbít | automatické nacenění multicoloru | platí |
| 14 | Nestavět malovátko barev, přijmout 3MF s paint daty | měsíce práce vs. dny; cílovka Bambu Studio nebo Orca už používá | browserový color painter | platí |
| 15 | Individuální nabídka jako jedna obecná úniková cesta | jedna feature pokryje šest případů včetně zákazníků bez souboru | zvláštní cesta pro každý případ | platí |
| 16 | Platba 100 % předem u automatické nabídky | díl na míru je jinak odpad; záloha znamená dvě platby a vymáhání doplatku | záloha u všech objednávek | platí |
| 17 | Certifikace zdarma místo výdělkové rampy bez IČO | soustavnost se posuzuje podle vzorce chování a úmyslu; rampa riziko nesnižuje dost | 3 zakázky / 8 000 Kč bez IČO | platí |
| 18 | Transparentní rozpad ceny | konzistentní se způsobem, jak jsou stavěné ostatní projekty; zákazník vidí tisk, dopravu i slevu zvlášť | doprava zabalená v ceně jako u konkurence | platí |
| 19 | Práh dopravy zdarma ~1 000 Kč, ne 5 000 | práh, kterého nikdo nedosáhne, je dekorace; smyslem je, aby si člověk s objednávkou za 900 přihodil | vysoký práh | platí |
| 20 | Express = předběhnutí fronty, ne kurýr | žádné nové náklady, zpeněžuje volnou kapacitu; kurýr by omezil na Brno a okolí | expresní doprava | platí |
| 21 | Kapacitní brána expresu měří **zásahy obsluhy**, ne hodiny | jeden dvacetihodinový tisk přes noc je v pořádku, pět čtyřhodinových podložek ne | brána podle hodin tisku | platí |
| 22 | Hodnotový strop automatu | prémie za bezbariérovost funguje na malých objednávkách; riziko se kryje — špatně naceněná malá zakázka stojí padesátikorunu, velká tisíce | automat na jakoukoli částku | platí |
| 23 | Node scope na dotazech od prvního dne | zpětné zavedení multi-tenancy není přidání sloupce, ale audit každého query | jednouživatelský admin | platí |
| 24 | Makerské UI uvnitř administrace, datový šev zachován | samostatný portál je práce bez uživatele; přesun obrazovek je levný, přepis datové vrstvy ne | samostatná `/maker` aplikace hned | platí |
| 25 | Sledování přes tokenizovanou URL bez účtu | registrace u jednorázové objednávky je překážka v nejcitlivějším místě trychtýře | uživatelské účty | platí |
| 26 | Pevný stavový automat, konfigurovatelné jen politiky | obecný workflow engine stojí násobek a nikdy se nepoužije jinak než jedním způsobem | konfigurovatelné přechody | platí |
| 27 | ~~Amortizace nevstupuje do ceny, sleduje se bokem~~ | — | — | **zrušeno #33** |
| 28 | Jedno jméno pro obě fáze, žádný rebranding | rebranding zahodí vybudované SEO, což je deklarovaný kanál | solo značka → přejmenování na síť | platí |
| 29 | Validace obsahuje celý automatický quote | landing s formulářem by validoval obecnou poptávku po tisku, kterou obsluhuje dvacet firem — ne diferenciaci | landing + poptávkový formulář jako v0 | platí |
| 30 | Contribution margin místo hrubé marže, CAC zvlášť | 30% hrubá marže může vypadat dobře, zatímco každá objednávka pálí peníze | hrubá marže | platí |
| 31 | Kill criterion má strop na reklamní spend | 10 objednávek za 20 000 Kč není validace | jen počet objednávek | platí |
| 32 | Síť je výsledek rozhodovací brány, ne v2 | síť řeší kapacitní problém; když ho nemáš, neřeší nic. Druhý stroj je výchozí varianta | síť jako automatické pokračování | platí |
| 33 | **Amortizace jako parametr s explicitní dobou návratnosti** (`cena stroje / návratnost v h`) | dělá z předpokladu číselník místo skryté volby; v0 a hobby režim = ∞, tedy 0 Kč/h; při rozhodování o 2. stroji se přepne na reálnou hodnotu | ruší #27 | platí |
| 34 | **Marže se aplikuje na celý výrobní náklad, ne na cenu materiálu** | model z Prusa blogu počítá marži jako 30 % z materiálu — u téže práce vyjde marže 1 Kč u levného PLA a 393 Kč u karbonu; tvoje riziko a čas s cívkou nesouvisejí | marže z ceny materiálu | platí |
| 35 | **Sazba práce 300 Kč/h — operátorská, ne vývojářská** | byznys zpeněžuje stojící stroj a poloprázdný večer; při vývojářské sazbě je model ztrátový. Benchmark: co by stál brigádník. Test poctivosti: přežije to, až to nebudeš dělat ty? | vlastní čas jako „zadarmo" nebo za vývojářskou sazbu | platí |
| 36 | **Handling se měří ze stavového automatu, ne stopkami** | časová razítka přechodů `accepted→printing→printed→packed→handed_over` dají handling z prvních ~20 reálných zakázek; měření nemusí předcházet spuštění, jen verdiktu | stopky a měření před spuštěním | **zrušeno #60** |
| 37 | **Provoz a validace jsou dva oddělené režimy** | při 1 zakázce měsíčně nezměříš FPY, konverzi po pásmech ani podíl souborů v automatu; nesměšovat, jinak vznikne rok „nějak to funguje" bez jediného čísla | jeden režim s průběžnou validací | platí |
| 38 | **Fixní náklady v idle téměř nulové** | hobby režim je přijatelný koncový stav, takže musí být bezúdržbový; projekt nezabije rozhodnutí, ale otrávenost z měsíčních nákladů bez odezvy | optimalizovat jen procenta z transakcí | platí |
| 39 | **Platební brána bez měsíčního paušálu** | při 1–2 objednávkách měsíčně sežere paušál 200 Kč marži ze dvou zakázek; ruší dřívější doporučení Comgate podle procent | brána vybraná podle transakčních procent | platí |
| 40 | **Risk checkboxy nezaškrtnuté** | explicitní acknowledgement je silnější produktově i právně, a právě o ten argument při reklamaci jde | předzaškrtnuté jako u PCBWay | platí |
| 41 | **Zkušební kus jako fáze objednávky, ne e-mailová domluva** | trh iteraci běžně dělá, ale e-mailem přes 4–6 zpráv; u nás je to stav v objednávce se zamčenou cenou obou fází | nová poptávka pro dávku | platí |
| 42 | **Slibujeme věrnost modelu, ne lícování** | zákaznické modely bývají laděné empiricky na jiné tiskárně a nesou skrytou kompenzaci — přesnější tisk je pak fitově horší. Konkrétní podoba rozdělení „vada tisku vs. vada modelu" | garance lícování | platí |
| 43 | **STEP jako univerzální vstup** | neutrální formát, exportuje ho každý, nezavazuje k ekosystému; nativní formáty s historií dospecifikovat, až je zákazníci budou umět dodat | vyžadovat nativní CAD formáty | platí |
| 44 | **Ceník, specifikace a log jako tři oddělené artefakty** | jednorázový osmdesátistránkový dokument se přestane udržovat, když se v něm mění cena filamentu; různá frekvence změn = různé soubory | jeden živý dokument | platí |
| 45 | **Roadmapa spouštěči, ne termíny** | datum ve firmě o jednom člověku s kolísavou kapacitou nikdy nesedí a nutí ho posouvat | kvartální roadmapa | platí |
| 46 | **Revize dokumentace se spouští událostí, ne kalendářem** | při 12 objednávkách ročně nemá měsíční revize co revidovat; hodina nad specifikací musí něco vrátit | pravidelná měsíční revize | platí |

---

## Nezapsaná, ale platná pravidla

Věci, které nejsou rozhodnutím o produktu, ale o tom, jak se rozhoduje:

**Test návratnosti featury.** Featura, která ušetří 5 minut na objednávce, ušetří při 1–2 objednávkách měsíčně **12 Kč měsíčně**. Cokoli nad jeden večer práce se při tom objemu nikdy nevrátí. Při 30 objednávkách měsíčně ušetří tatáž featura 375 Kč a týden práce se zaplatí. **Stejná featura má podle režimu dvacetipětinásobně jinou hodnotu** — přepočítej ji proti aktuálnímu objemu, ne proti tomu, ve který doufáš.

Výjimka: cokoli, co snižuje fixní náklady nebo tření natolik, že provoz zůstane bezúdržbový, se staví vždy (viz #38).

**Známé riziko projektu.** Spadl už jednou pod stůl (leden 2026) kvůli kapacitě, ve prospěch jednodušších věcí ve frontě. Nebylo to špatné rozhodnutí — byla to absence pravidla. Test výše to pravidlo dodává.

---

## Doplněno po revizi v1.2 → v1.3

| # | Rozhodnutí | Zdůvodnění | Zamítnutá alternativa | Stav |
|---|---|---|---|---|
| 47 | **Handling rozložen na komponenty před měřením** (`order_fix`, `plate`, `piece`, `pack`, `shipping_trip`, `postprocessing`) | jedno číslo „30 min" směšuje aktivní práci, práci na podložku a logistickou cestu; po 20 objednávkách bys věděl, že handling je 23 minut, ale ne co z toho jde batchovat | jeden souhrnný `handling_fix` | platí |
| 48 | **`shipping_trip` se při nízkém objemu nealokuje** | jedna cesta na jednu zásilku znamená plný náklad; handling na objednávku je v hobby režimu horší, ne lepší. Alokace začne fungovat od několika zásilek týdně | alokovat cestu vždy | platí |
| 49 | **`poplatek_priprava` → `small_order_surcharge`** | příprava je už v `cena_tisku` přes `handling`; původní název tvrdil zákazníkovi opak a účetně to bylo dvojí účtování | ponechat název | platí |
| 50 | **`min_print_price` se vztahuje na `cena_tisku`, ne na částku u pokladny** | bez explicitní definice se „minimální objednávka 250 Kč" za rok přečte jako nejnižší možný účet, přičemž checkout floor je 335–385 Kč | nedefinovat | platí |
| 51 | **`koef_kvality` zrušen; čas se bere ze skutečného slice** | slicer řekne 6 h 12 min přesně; násobit to ručním koeficientem znamená zahodit výhodu deterministické ceny. Obchodní přirážka smí existovat, ale musí se tak jmenovat a stát vedle času | koeficient nad strojovým časem | platí |
| 52 | **Referenční profily per materiál × kvalita, ne jeden generický PLA** | PETG má jiné rychlosti a teploty; nacenit ho podle PLA slice je systematicky vedle. Invariant zní „nezávislý na stroji", ne „nezávislý na materiálu". Slicuje se líně, až pro zvolenou kombinaci | jeden globální referenční profil | ruší část #7 |
| 53 | **Ve v0 slicer-native arrange, ne vlastní packing engine** | Orca má vlastní arrangement; psát k němu paralelní 2D packing je práce navíc bez odpovídajícího přínosu. Princip „slicuj podložku" zůstává, implementace se zjednodušuje | vlastní 2D packing v v0 | platí |
| 54 | **STEP není blocker vydání v0** | přináší tesselaci s deterministickou tolerancí, jednotky, sestavy, náhled a novou plochu na selhání; úkolem v0 je ověřit instant quote, ne pokrýt formáty | STEP jako součást v0 | platí |
| 55 | **Kampaň měří „first-order profitable", ne „valid / invalid"** | byznys s CM 120 Kč, CAC 170 Kč a třetinovou opakovaností je zdravý, jen se nezaplatí z první objednávky; binární brána by ho zabila | binární brána `CAC ≤ CM` | zpřesňuje #31 |
| 56 | **Opakovanost je hlavní otázkou placené akvizice, ne celého projektu** | může vyjít vysoký podíl organiky, drahý CAC, nízká opakovanost, zdravá CM — a stroj se přesto vytěžuje. Pro cíl „zpeněžit nevyužitou H2S" je to platný výsledek | opakovanost jako hlavní otázka projektu | platí |
| 57 | **Báze pro `rezerva_pretisk` definovaná explicitně** — `material + machine + handling_plate + handling_piece + postprocessing` | „5 % variabilního nákladu" si každý implementátor vyloží jinak; položky, které se při přetisku chyceném doma neopakují (`order_fix`, `pack`, `shipping_trip`), do báze nepatří. Odmítnutí po doručení kryté není a sedí zatím v marži | volná formulace „z variabilního nákladu" | **zrušeno #59** |
| 58 | **Spouštěč `small_order_surcharge` podle gramáže je prozatímní** | gramáž je proxy převzatá z trhu, ale materiál tvoří jen malou část nákladu; pravděpodobnějším spouštěčem je `cena_tisku < X` nebo `handling / cena_tisku > Y`. Pro v0 zůstává 100 g a sleduje se jako metrika | fixovat 100 g jako strukturu | platí |
| 59 | **`rezerva_pretisk` používá peněžní `handling_pretisk` se stejnými násobnostmi jako hlavní handling** | původní báze sčítala časové parametry přímo s Kč a vynechávala počty podložek a kusů; správně se opakovaná práce nejdřív ocení `sazba_prace_h` | sčítat neoceněné délky s peněžními náklady | ruší #57 |
| 60 | **Aktivní handling se měří přes `HandlingSession`, stavové časy jen průchod procesu** | interval mezi stavy obsahuje tisk, frontu, čekání i dopravu a není pracovním časem; timer nebo strukturovaný ruční zápis zachová komponentu i jmenovatele pro dávky | odvozovat práci z rozdílu stavových časů | ruší #36 |
| 61 | **Expresní příplatek stojí odděleně od `cena_tisku_zaklad`** | práh dopravy zdarma se musí vyhodnotit před expresním příplatkem, jinak si zákazník koupí dopravu zdarma připlacením za rychlost | aplikovat `koef_express` před vyhodnocením prahu dopravy | platí |
| 62 | **Cache slice je vždy klíčovaná i obsazeností podložky** (`parts_per_plate`) | čas a materiál jednoho kusu se liší od plné podložky; bez obsazenosti by cache mohla vrátit jinou závaznou cenu | klíč jen soubor + profil | platí |
| 63 | **Přetisk vzniká jako nový navázaný `Job`, ne návrat stavu původního** | terminální `failed` / `qc_rejected` zachová FPY a audit; `replaces_job_id` dovolí dokončit zaplacenou objednávku bez přepsání historie | vracet tentýž job do `printing` | platí |
| 64 | **Fulfilment a platby mají oddělené stavy; storno je explicitní větev před odesláním** | individuální nabídka má zálohu a doplatek, takže jediný stav `paid` neumí popsat start výroby ani refundaci; odvozený `payment_status` drží účetní pravdu | jeden platební stav na objednávce | platí |
| 65 | **Fázovaná objednávka má `OrderPhase` a více zásilek** | vzorek musí být doručen a potvrzen před výrobou dávky; jedna zásilka na objednávku tento flow neumí reprezentovat ani ocenit | jedna zásilka nebo dvě nesouvisející objednávky | zpřesňuje #41 |
| 66 | **Platby jsou kolekce transakcí s rolemi `full` / `deposit` / `balance`** | záloha a doplatek musí jít samostatně spárovat, refundovat a auditovat; jeden sloupec na objednávce ztrácí historii i částečné stavy | jedna platba na objednávku | zpřesňuje #16 |
| 67 | **Doprava, obal a balicí práce se sčítají přes všechny plánované zásilky** | sample a batch odcházejí v jiný čas; jedna sazba by zamčenou cenu i CM systematicky podhodnotila | účtovat jednu zásilku na objednávku | zpřesňuje #65 |
| 68 | **Revize modelu po sample vytváří `OrderRevision` a novou cenu batch fáze** | změna geometrie může změnit materiál, čas, obsazenost podložky i dopravu; původní závazná cena platí jen pro původní model | tisknout revidovaný model za původní cenu | zpřesňuje #41 |
| 69 | **Poslední fáze řídí všechny mezistavy agregátní objednávky** | sample shipment je dílčí, ale batch musí vyvolat `qc_passed → ready_to_ship → shipped → delivered`; přímý skok by porušil pevnou topologii a audit | skok `in_production → delivered` | zpřesňuje #26 a #65 |
| 70 | **`ReferenceProfile` a `MachineProfile` jsou oddělené entity i verze** | zákaznická cena musí být nezávislá na stroji, produkční G-code naopak konkrétní model a trysku potřebuje; společná matice by nechala ceny driftovat mezi stroji | jeden typ profilu pro quote i výrobu | zpřesňuje #52 |
| 71 | **`SliceResult` klíč obsahuje i `profile_kind`** | reference a machine profily mají oddělené tabulky a mohou mít stejná lokální čísla verzí; bez typu by se jejich cache klíče mohly srazit | společný neoznačený `profile_version` | **zrušeno #72** |
| 72 | **Cache používá globálně unikátní profile/calibration revision ID** | dva kusy stejného modelu mohou mít jiné flow a XY kompenzace a dvě tabulky stejná lokální čísla verzí; immutable revision ID zabrání oběma kolizím a job je snapshotuje | typ + lokální číslo verze nebo cache jen podle `MachineProfile` | ruší #71, zpřesňuje #70 |
| 73 | **Reklamace je samostatný `Claim`, ne zpětný přechod objednávky** | záruka může být uplatněná po `completed`; přepis terminálního fulfilment stavu by kazil historii a provozní metriky | nechat objednávky navždy `delivered` nebo vracet `completed → disputed` | platí |
| 74 | **Sample-only výsledek končí jako `partially_fulfilled`** | odmítnutá revize ponechá dodaný vzorek a refunduje jen nečerpaný batch; není to ani úplné `completed`, ani úplné `refunded` | nechat objednávku viset v `in_production` | zpřesňuje #64, #65 a #68 |
| 75 | **`ShipmentPlan` dimenzuje celé množství podle objemu i hmotnosti** | 20 jednotlivě malých dílů může překročit krabici nebo hmotnost; závazná cena musí znát počet balíků před checkoutem | kategorie jen z bbox největšího dílu | zpřesňuje #67 |
| 76 | **Slice cache klíčuje kanonický `ModelGeometry`, ne celý upload** | dvě tělesa jednoho STEP souboru mají stejný file hash, ale jiný čas, materiál i bbox; extrahovaná geometrie dává každému `OrderItem` vlastní bezpečný klíč | `sha256(ModelFile)` pro všechny vybrané části | zpřesňuje #62 |
| 77 | **Zvýšení ceny revize se platí před aktivací batch** | přijetí cenového rozdílu bez capture by pustilo dražší výrobu proti původní záloze; `revision_amount_due = 0` zachová předplacení změny | doplatek revize až před odesláním | zpřesňuje #16 a #68 |
| 78 | **Paleta je union inventáře jen společně způsobilých strojů** | barva na stroji, který nesplní objem, profil, trysku nebo tier, není pro danou zakázku dostupná; globální union by prodal nevyrobitelnou kombinaci | globální union všech barev v síti | zpřesňuje #8 |
| 79 | **Selhání batch po doručeném sample se vypořádá po fázi** | vzorek byl dodaný a spotřebovaný, takže plná refundace objednávky je chybná; bez náhradního jobu se batch zruší a agregát skončí `partially_fulfilled` | vždy `cancelled → refunded` celé objednávky | zpřesňuje #63 a #74 |
| 80 | **`rezerva_pretisk` zahrnuje opakovanou amortizaci** | zmetek spotřebuje životnost stroje stejně jako úspěšný tisk; při nenulové amortizaci by jinak cena neunesla druhý stroj ani síťovou ekonomiku | rezervovat jen materiál, variabilní stroj a práci | zpřesňuje #59 |
| 81 | **Odeslání hlídá zůstatky aktuálního cenového snapshotu, ne název platebního stavu** | snížení ceny může po vrácení přeplatku legitimně nechat `payment_status = partially_refunded`; nuly `amount_due` a `refundable_balance` dokazují, že není co vybrat ani vrátit | vyžadovat vždy `payment_status = paid` | zpřesňuje #64, #68 a #77 |
| 82 | **Cenová podlaha zahrnuje dotovanou dopravu před marží a hrubuje poplatek brány** | doprava zdarma ani procentní/fixní poplatek nesmějí změnit nominálně maržovou objednávku na zápornou CM; práh se vyhodnotí před subvencí, aby se nekvalifikovala sama | odečíst dopravu a bránu až při reportingu CM | zpřesňuje #19, #34, #61 a #67 |
| 83 | **Jedna retence platí pro všechny zdrojové formáty `ModelFile`** | 3MF a zvlášť STEP mohou nést přinejmenším stejně citlivá data jako STL; přípona nesmí rozhodovat, zda se upload někdy smaže | plánovat mazání jen pro STL | platí |
| 84 | **Způsobilost i rezervace inventáře jsou gramážové, ne booleovské** | přítomnost barvy neznamená dost materiálu pro celou dávku; `available_g` odečítá aktivní rezervace a capture smí začít až po atomické rezervaci `required_material_g` na jednom uzlu; další maker ji musí převzít před nabídkou jobu | filtrovat jen podle přítomnosti barvy a rezervovat až po platbě | zpřesňuje #78 |
| 85 | **Minimální reprodukční artefakt se drží nejméně do `claim_until`** | zdrojový CAD lze po 90 dnech smazat, ale claim v podporované lhůtě musí stále umět vytvořit shodný náhradní job z kanonické geometrie a immutable slice vstupů | držet celý zdroj do konce claimů nebo po 90 dnech ponechat jen nereprodukovatelný hash | zpřesňuje #73 a #83 |
| 86 | **Osobní odběr není bez vlastního privacy-preserving workflow součástí ekonomiky** | navržený checkout posílá přes dopravce a maker zákazníka nevidí; geografickou úsporu proto nelze započíst do síťové brány dřív, než existuje anonymizované předání | přičíst síti ~105 Kč úspory bez implementovaného procesu | zpřesňuje #32 |
| 87 | **Každý náhradní job potřebuje novou produkční rezervaci** | materiál původního pokusu je po přijetí jobu spotřeba a jeho kapacita proběhla; `ReplacementRequest` drží recovery povinnost, ale job vznikne až s čerstvou gramáží a intervaly | znovu použít spotřebovanou rezervaci původního jobu | zpřesňuje #63, #73 a #84 |
| 88 | **Revize batch vždy znovu ověřuje a nahrazuje produkční rezervaci** | nižší cena nemusí znamenat méně materiálu ani kratší strojový čas; aktivace závisí na čerstvé gramáži a kapacitě nezávisle na tom, zda vznikl doplatek | revalidovat jen zdraženou revizi před capture | zpřesňuje #68 a #77 |
| 89 | **`ProductionReservation` atomicky spojuje materiál i strojové intervaly** | dost filamentu nebrání dvěma souběžným checkoutům slíbit tentýž termín jednoho stroje; oba zdroje se musí zamknout, držet, převádět i uvolnit jako jeden celek | rezervovat před platbou jen inventář | zpřesňuje #21 a #84 |
| 90 | **`PrintConfigRevision` je součástí každého slice klíče** | samostatně volená výplň mění toolpath, gramáž, čas i pevnost; stejná geometrie a kvalita proto nesmí sdílet cache mezi 10/20/40 % | skrýt výplň do profilu kvality nebo ji z klíče vynechat | zpřesňuje #51, #62 a #72 |
| 91 | **Rezervace zdrojů vychází z machine-specific candidate odhadu, ne z reference slice** | heterogenní stroje mají jiné podložky, rychlosti, podpěry i kalibrace; reference zůstává jen pro zákaznickou cenu a každý kandidát prokazuje vlastní gramáž a intervaly | kopírovat do všech rezervací metriky referenčního stroje | zpřesňuje #5, #7 a #89 |
| 92 | **Ztracená nebo vrácená běžná zásilka otevírá claim už ze stavu `shipped`** | bez pre-delivery incidentu nemá nedoručená zaplacená objednávka cestu k nové zásilce, přetisku ani refundu | povolit claim až po `delivered` | zpřesňuje #64, #65 a #73; náhradní Shipment viz #133 |
| 93 | **Částečná refundace je opakovatelná smyčka řízená součtem** | express refund a pozdější claim refund mohou být dvě dílčí operace nad jedním capture; plný `refunded` platí až při rovnosti vrácené a zachycené částky | druhou refundací vždy označit platbu jako plně vrácenou | zpřesňuje #64 a #66 |
| 94 | **Job může selhat i před začátkem tisku** | G-code generation, porucha stroje nebo odstoupení makera po acceptance nesmějí blokovat zaplacenou objednávku; `failure_stage` vede všechny fáze přes stejný `ReplacementRequest` nebo storno | povolit `failed` jen z `printing` | zpřesňuje #63 a #87 |
| 95 | **Job dědí profile/calibration/config snapshot z produkční rezervace** | aktivní kalibrace se může mezi checkoutem a acceptance změnit; jiné vstupy by zneplatnily rezervovanou gramáž a intervaly, takže změna vyžaduje atomický re-estimate/re-reserve | načíst při acceptance právě aktivní kalibraci | zpřesňuje #72, #89 a #91 |
| 96 | **Gross-up sčítá poplatek každého capture v `PaymentSchedule`** | individuální záloha a doplatek platí fixní složku brány dvakrát a revizní balance přidává další transakci; jediný poplatek by porušil cenovou podlahu | hrubovat jen agregátní `cena_celkem` jedním fixním poplatkem | zpřesňuje #66, #77 a #82 |
| 97 | **Přijetí jobu materiál alokuje, spotřeba začíná až stavem `printing`** | pre-print failure nic nevytiskl a musí vrátit celou gramáž; po startu se zapisuje skutečná spotřeba a vrací jen zbytek. Ruší pouze předpoklad v odůvodnění #87, že materiál je spotřebovaný už při acceptance; požadavek nové rezervace pro replacement zůstává | commitnout celou gramáž do spotřeby při přijetí jobu | zpřesňuje #87 a #94 |
| 98 | **Čekání na potvrzení sample má deadline a automatické dílčí vypořádání** | mlčící zákazník nesmí navždy blokovat předplacený materiál a kapacitu batch; po timeoutu se zdroje uvolní, batch část refunduje a doručený sample zůstane `partially_fulfilled` | nechat `awaiting_confirmation` a held rezervaci bez expirace | zpřesňuje #41, #65 a #74 |
| 99 | **Incident mezifázové zásilky zachovává agregát v `in_production`** | ztracený sample není finální shipment a jeho náhrada nesmí vyvolat zakázaný skok agregátu do `shipped`; jen finální/jediná zásilka drží během recovery stav `shipped` | všechny shipment incidenty řešit v agregátu `shipped` | zpřesňuje #69 a #92 |
| 100 | **Reprodukční vstupy se verzují per accepted job po finálním routingu** | rerouting může změnit profil i kalibraci; claim musí vybrat snapshot jobu, který skutečně vyrobil doručenou fázi, ne původně rezervovaný stroj | vytvořit jediný machine-specific artefakt už při přijetí objednávky | zpřesňuje #85 a #95 |
| 101 | **Nezaplacený doplatek má deadline a explicitní `OrderSettlement`** | hotový zakázkový výrobek ani pohledávka nesmějí viset navždy; settlement doloží earned část zálohy, refund přebytku a write-off nevymáhané ceny místo automatického propadnutí či plného vrácení | čekat bez termínu nebo vždy refundovat celý capture | zpřesňuje #16, #64 a #66 |
| 102 | **Reship vyžaduje fyzicky vrácený nebo nalezený výrobek po novém QC** | ztracenou zásilku nelze vložit do nového balíku; bez potvrzené custody musí claim vytvořit rezervovaný reprint nebo refund | povolit `resolved_reship` přímo ze stavu `lost` | zpřesňuje #92 |
| 103 | **Quote používá verzovaný `shipping_trip_pricing_divisor`, skutečnost jen reporting** | budoucí počet zásilek na cestě při nabídce neexistuje; v v0 divisor 1 drží cenu deterministickou a reálný společný trip se promítne až do realized CM | dělit závaznou cenu pozdějším skutečným počtem zásilek | zpřesňuje #47 a #48 |
| 104 | **Routing exhaustion ukončí nepřijatý job jako `cancelled`** | job v `created` nesmí zůstat neterminální po odmítnutí/timeoutu všech nabídek a refundu objednávky | ponechat jediný přechod `created → accepted` | zpřesňuje #63 a #89 |
| 105 | **Poškození nebo ztráta po tisku a před prvním handoff jde přes `failed` a `recovery_pending`** | schválený díl se může zničit při balení; před první parcelou musí agregát opustit odesílatelný stav a čekat na QC rezervované náhrady nebo storno | mít failure větev jen během tisku a před QC | zpřesňuje #63, #87 a #94; pozdější parcely viz #131 |
| 106 | **Opožděný capture po TTL musí zdroje znovu získat, nebo se kompenzovat refundem** | úspěšný webhook není důkaz, že expirovaná gramáž a intervaly stále patří checkoutu; bez atomické reacquisition nesmí vzniknout potvrzená objednávka ani Job | každý pozdní capture změnit na `held` | zpřesňuje #84 a #89 |
| 107 | **Před potvrzením fitu se rezervuje pouze sample, ne batch** | cena batch zůstává zamčená, ale jeho fyzické zdroje nesmějí být blokované během výroby, přepravy, ztráty nebo reprintu vzorku; batch se rezervuje čerstvě až při aktivaci | držet batch zdroje od prvního checkoutu do doručení sample | zpřesňuje a v části rezervace nahrazuje #98 |
| 108 | **Fyzický handoff je atomicky svázaný s finančně autorizovanou fází** | samotný stav Jobu nesmí umožnit vydat finální výrobek před doplatkem; mezifázový sample má vlastní explicitní phase guard | kontrolovat finance jen na agregátním `ready_to_ship → shipped` | zpřesňuje #64 a #105 |
| 109 | **QC fáze je bariéra přes všechny povinné fulfilment sloty** | STEP sestava, více barev nebo podložek vytváří více jobů; první schválený job nesmí odeslat nekompletní fázi | posunout agregát libovolným `Job.qc_approved` | zpřesňuje #69 a #105 |
| 110 | **Každý Shipment claimového reprintu používá jednorázové `ReplacementFulfilmentAuthorization`** | objednávka si při shipment incidentu i post-delivery reklamaci zachová svůj stav, ale úplná QC-approved množina náhradních Jobů a slotů musí mít auditovanou cestu ke každému navázanému Shipmentu | obejít handoff guard nebo vracet Order do `ready_to_ship` | zpřesňuje #92 a #108; úplný set viz #130 |
| 111 | **Obchodní storno ukončí i každý přijatý pre-handoff Job jako `cancelled`** | zrušení rozpracované objednávky nesmí ponechat živý job ani předstírat výrobní poruchu; rezervace se vypořádá podle skutečné spotřeby a závod s handoff je zamknutý | povolit `cancelled` jen z `created` nebo použít `failed` | zpřesňuje #63, #97 a #104 |
| 112 | **Zlevňující revize atomicky spouští idempotentní refund skutečného přeplatku** | pouhé zvýšení `refundable_balance` by navždy blokovalo handoff; přijetí revize musí proti čistě zachycené částce vytvořit konkrétní refund, pokud sleva převýší budoucí doplatek | očekávat ruční refund později nebo vracet celý rozdíl bez ohledu na capture | zpřesňuje #68, #81 a #93 |
| 113 | **Doručení a dílčí vypořádání se agregují přes `FulfilmentSlot` a shipment lineage** | jediná fáze může mít více parcel; první doručená a druhá refundovaná je částečně splněná objednávka, ne plný refund ani kompletní delivery | agregovat jen podle celých `OrderPhase` | zpřesňuje #65, #69, #75 a #92 |
| 114 | **Claim je terminální až po výsledku všech svých slotů** | vybraná náprava může selhat na kapacitě nebo v další přepravě; každý slot musí mít doručený reprint/reship nebo úspěšný refund a kombinace končí `resolved_mixed` | uzavřít Claim volbou operátora, prvním balíkem nebo jedním child refundem | zpřesňuje #92 a #110; per-slot agregace viz #133 |
| 115 | **Painted nebo multimaterial 3MF blokuje automatickou nabídku v v0/v1** | jednobarevný konfigurátor neumí ocenit další barvy ani machine-specific purge; metadata se nesmějí ztratit při importu | přijmout každý 3MF do direct flow a paint ignorovat | zpřesňuje #13 a #14 |
| 116 | **`recovery_pending` má explicitní terminální finanční větev** | post-QC náhrada nemusí získat kapacitu; agregát bez dodaného slotu musí umět dojít k refundu a s dodanou částí k `partially_fulfilled` | ponechat recovery bez cancellation přechodu | zpřesňuje #105 a #113 |
| 117 | **Nedoručená plně refundovaná fáze končí `cancelled_refunded`** | ztracený sample bez jediného doručeného slotu není `partially_fulfilled`, ale jeho fáze přesto potřebuje terminální stav odpovídající agregátu `cancelled → refunded` | ponechat `shipped` nebo předstírat dílčí plnění | zpřesňuje #99, #113 a #114 |
| 118 | **Post-delivery quality Claim lze před handoff auditovaně stáhnout do `withdrawn`** | zrušený request a Job nesmějí ponechat Claim ani retenční hold otevřený; shipment incident musí naproti tomu vždy vyřešit nedoručené sloty | zrušit jen Job s `claim_withdrawn` nebo stáhnout shipment incident | zpřesňuje #111 a #114; kind guard viz #132 |
| 119 | **OBJ se nepropaguje jako direct flow bez úplného datového kontraktu** | kanonizace, cache ani společná source retence OBJ nejsou specifikované; v0/v1 slibují jen skutečně pokryté STL/3MF a později STEP | přijmout OBJ jen podle osamoceného řádku tabulky | zpřesňuje #11 a #83 |
| 120 | **Výrobní handoff se provádí per Shipment a Job nikdy nekříží parcel plány** | jedna výrobní úloha nemůže projít stavem `handed_over` dvakrát; běžná nebo reprint parcela atomicky předá všechny své packed Joby a další plán má vlastní Job množinu | volat `handoff_job` opakovaně pro tentýž Job | zpřesňuje #108 a #113; custody reship výjimka viz #128 |
| 121 | **Claimový reprint sealne nové machine-specific vstupy po čerstvém routingu** | doručený artefakt je důkaz a zdroj kanonické geometrie, ale jiný náhradní stroj potřebuje vlastní candidate estimate, profil, kalibraci a production slice | kopírovat celý snapshot původního stroje | zpřesňuje #95, #100 a #110 |
| 122 | **Terminální objednávka bez doručení dostane deadline reprodukčního artefaktu** | refundovaný nebo zrušený Job nikdy nevytvoří `claim_until`, ale kanonická geometrie nesmí zůstat rekonstruovatelná navždy; terminální čas spustí samostatnou lhůtu prodloužitelnou jen holdem | nastavovat `reproduction_delete_after` pouze po doručení | zpřesňuje #83 a #85 |
| 123 | **Capture rezervuje atomický `PhaseReservationSet` přes všechny itemy a Joby** | STEP assembly a různobarevné itemy mohou vyžadovat více candidate odhadů i strojů; platba nesmí projít s kapacitou jen pro první geometrii | jedna `ProductionReservation` vybraného stroje bez úplného phase plánu | zpřesňuje #84, #89, #91 a #120 |
| 124 | **QC rejection je vlastní terminální stav Jobu** | zamítnutá fotografie není obecná výrobní porucha a replacement flow už na `qc_rejected` spoléhá | deklarovat stav jen v próze mimo pevný automat nebo použít `failed` | zpřesňuje #69, #97 a #109 |
| 125 | **Settlement nejdřív zavře balance capture a pozdní úspěch celý refunduje** | provider webhook může doběhnout po marném deadline; nesmí znovu otevřít terminální objednávku, změnit immutable settlement ani nechat zákazníka zaplatit likvidovaný výrobek | ponechat `pending → captured` bez cutoff guardu | zpřesňuje #78, #79 a #103 |
| 126 | **Refund expresního SLA nejdřív odebere příplatek z aktivní smluvní ceny** | samotný refund proti snapshotu obsahujícímu příplatek vytvoří falešný `amount_due` a zablokuje handoff základní objednávky | vrátit příplatek bez následné immutable revize ceny | zpřesňuje #61, #68 a #81 |
| 127 | **Post-QC storno terminálně vypořádá i `OrderPhase`** | Order v `ready_to_ship` má fázi stále `qc_passed`; bez phase transition by pod terminálním agregátem zůstala živá fáze | ukončit jen Order a Joby | zpřesňuje #69, #105 a #111 |
| 128 | **Nalezený nebo vrácený obsah má custody-backed `handoff_reshipment`** | původní Job už byl jednou předán a nesmí znovu projít `packed → handed_over`; nový Shipment potřebuje vlastní jednorázovou autorizaci založenou na custody a re-QC | znovu přepnout původní Job nebo použít reprintovou autorizaci | zpřesňuje #102, #110 a #120 |
| 129 | **Claim refund nejdřív odebere hodnotu slotů z aktivní smluvní ceny** | samotný refund sníží net capture proti nezměněnému contract total a vytvoří falešný dluh; immutable `claim_slot_credit` zachová nulové `amount_due` | refundovat sloty bez `PriceAdjustment` | zpřesňuje #81, #93, #113 a #126 |
| 130 | **Claimový reprint rezervuje a vytváří úplný set Jobů pro všechny dotčené sloty** | jedna ztracená parcela může agregovat více Jobů, itemů a barev; jediný request/Job nesplní handoff bariéru celé náhradní zásilky | vytvořit náhradu jen pro jeden Job z původního Shipmentu | zpřesňuje #87, #110, #120 a #123 |
| 131 | **Recovery pozdější nepředané parcely zachová stav po prvním handoffu** | po prvním handoffu už finální/jediný agregát nemůže legálně couvnout ze `shipped` a sample agregát musí zůstat `in_production`; blokuje se jen dotčený pozdější plán a completion čeká na jeho náhradu nebo refund | přechod agregátu do `recovery_pending` nebo ponechání neblokované parcely | zpřesňuje #99, #105, #113 a #120 |
| 132 | **Undelivered `shipment_incident` nelze odmítnout ani stáhnout** | uzavření bez delivery, náhrady nebo refundu by ponechalo Order v `shipped`/`in_production` a navždy nesplnitelnou shipment barrier | sdílet `resolved_rejected | withdrawn` s quality claimem | zpřesňuje #92, #114 a #118 |
| 133 | **Incident náhradní zásilky se propaguje do původního Claim per slot** | vnořený Claim by mohl refundovat child parcelu, zatímco jiné child resolution a retenční hold rodiče zůstanou otevřené; jediný rodič musí agregovat doručení/refund všech slotů | automaticky otevřít nový Claim pro Shipment s `origin_claim_id` | zpřesňuje #92, #110, #114 a #130 |
| 134 | **`OrderPhase(kind = single)` existuje už ve v0** | společný handoff a QC guard vyžaduje phase stav i pro obyčejnou objednávku; entita nesmí vzniknout až s dvoufázovým workflow | phase-free v0 větev nebo `OrderPhase` jen ve v1 | zpřesňuje #65, #69, #108 a #127 |
| 135 | **Remedy workflow běží per `ClaimSlotResolution`, rodič je jen agregát** | jeden Claim může současně reshipovat vrácený slot, reprintovat ztracený a jiný refundovat; skalární parent stav nemůže reprezentovat smíšenou nápravu | jeden `reship_pending | reprint_pending | refund_pending` na celý Claim | zpřesňuje #114, #130 a #133 |
| 136 | **Aktivní Claim má exkluzivní vlastnictví každého slotu** | souběžný item a phase claim by jinak oba rezervovaly a odeslaly náhradu stejného plnění; finanční component credit samotný výrobní duplicitu nezastaví | hlídat pouze dvojí refund nebo slučovat až po vytvoření Jobů | zpřesňuje #113, #129, #130 a #135 |
| 137 | **Expres je do phase-scoped SLA povolený jen pro jedinou `single` fázi** | sample může splnit 24 h, ale agregát čeká na dopravu a potvrzení před batch; order-level deadline by proto automaticky vracel příplatek i při správné výrobě | účtovat expres přes celý sample/batch Order | zpřesňuje #61, #65, #126 a #134 |
| 138 | **Reject/withdraw ukončí i každý `ClaimSlotResolution`** | terminální rodič nesmí ponechat child `pending` a současně uvolnit ownership/retenci; odmítnutí i stažení potřebují atomickou per-slot dispozici a čistý scope | změnit jen stav rodičovského Claim | zpřesňuje #118, #132, #135 a #136 |
| 139 | **Refund incidentu nedoručeného sample ruší a kredituje i zamčený batch** | sample je prerequisite dávky a automatická objednávka už zachytila obě fáze; refund jen reklamované parcely by nechal předplacený batch bez aktivační cesty | použít pouze sample `claim_slot_credit` bez samostatného batch cancellation creditu | zpřesňuje #74, #92, #99 a #130 |
| 140 | **Confirmation timeout nejdřív zavře revision capture window** | pozdní provider success nesmí po stornu a uvolnění zdrojů znovu získat phase plan ani aktivovat zrušený batch | spoléhat jen na expiraci krátké rezervace a dovolit re-reserve | zpřesňuje #77, #88, #98 a #125 |
| 141 | **Každé finanční opuštění nedoručeného sample používá `SamplePrerequisiteAbort`** | zamčený předplacený batch ztratí výrobní cestu i po vyčerpání pre-handoff recovery, nejen po refundu shipment incidentu; obě větve musí volat společný batch cancellation credit | zvláštní batch credit pouze pro Claim refund | zpřesňuje #105, #116, #117 a #139 |
| 142 | **Odmítnutí revize terminálně uzavře sample a jeho deadline** | `partially_fulfilled` Order nesmí ponechat child `awaiting_confirmation` ani živý timeout, který později znovu spustí settlement | ukončit jen revizi, batch a agregát | zpřesňuje #68, #74, #98 a #140 |
| 143 | **Storno neaktivovaného batch má vlastní `batch_cancellation_credit`** | refund bez snížení aktivní smluvní ceny vytvoří falešný `amount_due` ve výši vrácené dávky a zablokuje `partially_fulfilled`; jeden idempotentní command musí obsloužit timeout, rejection i sample abort | refundovat batch proti původnímu `contract_total` nebo skrýt rozdíl settlementem | zpřesňuje #74, #81, #129, #139 a #141 |
| 144 | **`OrderPhase(kind = single)` má úplnou explicitní topologii** | v0 handoff už vyžaduje phase stav, takže neuvedený počátek a přechody by znemožnily implementovat běžnou objednávku podle pevného automatu | předpokládat phase-free flow nebo nevyřčenou shodu s batch | zpřesňuje #69, #108 a #134 |
| 145 | **Retenční deadline vzniká už při uploadu** | abandon nebo parse/preflight failure nemusí vytvořit Quote ani Order; bez počátečního `source_delete_after` by citlivý CAD a mezigeometrie zůstaly navždy | nastavit mazání až při expiraci nabídky nebo terminálním Orderu | zpřesňuje #83, #85 a #122 |
| 146 | **Pre-handoff Shipment má provider-confirmed cancellation topologii** | lokální storno Jobu ani parentu nezneplatní již vydaný carrier label; label musí projít `cancellation_pending`, void potvrdit dopravce a fyzický scan musí umět závod vyhrát | zrušit jen Job/Claim/Order a ponechat Shipment `planned | label_created` | zpřesňuje #118, #120, #127 a #142 |
| 147 | **Scan vítězící nad stornem používá úplný context-specific handler** | samotný přechod Shipmentu a Jobů by vynechal OrderPhase/Order side effects, spotřebu claim/reship autorizace nebo finanční reconciliation neautorizované custody, takže delivery barrier by zůstala nesplnitelná | při scanu změnit jen `Shipment.status` a packed Joby | zpřesňuje #120, #128, #135 a #146; finanční výjimka viz #157 |
| 148 | **Další sample parcela se předává z již odeslané sample fáze** | první handoff změní `OrderPhase` na `shipped`, ale mezifázový Order správně zůstává `in_production`; další `ShipmentPlan` proto potřebuje tuto explicitní dvojici guardů | po první parcele nadále vyžadovat `OrderPhase.qc_passed` nebo posunout celý Order do `shipped` | zpřesňuje #99, #108, #120 a #144 |
| 149 | **Předaná claimová náprava smí k refundu jen přes ověřený shipment incident** | přímý refund live replacement/reship parcely by mohl skončit současně s jejím doručením a zákazníkovi ponechat výrobek i peníze | povolit `replacement_shipped | reship_shipped → refund_pending` bez `lost | returned` události | zpřesňuje #114, #128, #133 a #135 |
| 150 | **Pre-handoff claim remedy refund čeká na provider-confirmed cancellation barrier** | `replacement_in_production` i `reship_pending` mohou mít live Job, Shipment nebo vydaný label; finanční větev smí začít až po jejich nevratném ukončení a vypořádání | přepnout child do `refund_pending` před voidem labelu nebo dovolit refundu závodit s handoffem | zpřesňuje #118, #130, #146 a #149 |
| 151 | **Customer-reference i QC fotografie mají upload-time retenční deadline** | povinná QC dokumentace a přílohy poptávky jsou osobní/provozní data stejně jako modely; bez `photo_delete_after` by přežily Quote, Order i Claim bez konce | odvodit uložení jen ze souhlasu s publikací nebo mazat pouze CAD/G-code | zpřesňuje #73, #83, #85 a #145 |
| 152 | **Pre-handoff replacement recovery používá tutéž cancellation barrier jako refund** | holé `replacement_in_production → recovery_pending → refund_pending` by obešlo void live labelu a dovolilo refundovat stále odesílatelnou náhradu | ukončit jen child stav bez zrušení Shipmentů, Jobů, rezervací a autorizací | zpřesňuje #130, #146, #149 a #150 |
| 153 | **Claim credit scope obsahuje jen child resolution zvolené k refundu** | mixed-remedy Claim smí jeden slot reprintovat, jiný reshipovat a třetí refundovat; kredit všech reklamovaných slotů by dal fulfilment siblingům současně výrobek i peníze | odvodit `claim_credit_scope` z celého parent Claimu nebo shipment incidentu | zpřesňuje #129, #135, #139 a #150 |
| 154 | **Phase a fulfilment sloty vznikají před initial checkout rezervací** | `PhaseResourcePlan` ani `PhaseReservationSet` nemohou odkazovat phase/slot ID vytvořená až následným capture webhookem; `quoted` scope dá rezervacím stabilní identity a capture jej pouze aktivuje | vytvořit `OrderPhase` až s `Order.confirmed` nebo rezervovat proti dočasným neentitním slotům | zpřesňuje #108, #123, #134 a #144 |
| 155 | **Initial `full | deposit` má explicitní capture cutoff a kompenzaci** | expirovaný nebo stornovaný checkout nesmí pozdním provider webhookem obnovit Order bez zdrojů ani ponechat zachycené peníze bez auditované refund cesty | `capture_authorized` a `capture_cutoff_at` pouze pro pozdější balance platby | zpřesňuje #84, #106, #123 a #125 |
| 156 | **Selhání revision-capacity reacquisition má vlastní plnou kompenzaci** | capture zvýšení ceny bez kompletního batch setu nesmí aktivovat revizi ani zůstat bez klasifikované refund cesty; nový pokus lze otevřít až po refundu a jen před deadlinem | vykázat jej jako initial checkout, rejection/expiry nebo ponechat Payment captured | zpřesňuje #106, #123, #140 a #155 |
| 157 | **Fyzický scan neautorizovaného unpaid handoffu vytváří reconciliation a settlement** | carrier custody je skutečná i když `awaiting_balance` nesplňuje běžný guard; void už nemůže uspět, ale systém nesmí předstírat autorizaci, pokračovat v capture ani refundovat jako by nic neodešlo | scan zahodit, ponechat `cancellation_pending`, obejít finanční guard nebo dokončit původní storno | zpřesňuje #125, #146 a #147 |
