# Tools

This directory is for maintained developer and operator utilities that are not
part of an application runtime. The
[`slicing-fixtures`](slicing-fixtures/README.md) corpus pins OrcaSlicer and the
supported H2S profile closure. No empty worker or speculative integration
projects belong here.
